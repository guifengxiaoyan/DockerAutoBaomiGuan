version: '3.8'

services:
  autobaomiguan:
    build: .
    container_name: autobaomiguan
    ports:
      - "3000:3000"
    volumes:
      - ./credentials.json:/app/credentials.json
    restart: unless-stopped
    environment:
      - TZ=Asia/Shanghai
